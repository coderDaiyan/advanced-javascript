const bonus = 10
function sum(first, second){
    const result = first + second + bonus
    // console.log(bonus);
    if(result > 10)
    return result
}

const output = sum(25, 25)
console.log(bonus);
console.log(output);